- name: install nginx and run
  hosts: web
  tasks: 
  - name: install nginx pacakage
    ansible.builtin.package:
      name: nginx
      state: present
  - name: run nginx
    ansible.builtin.service:
      name: nginx
      state: started    